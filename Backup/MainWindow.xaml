<Window x:Name="windowBackup" x:Class="Backup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Backup"
        mc:Ignorable="d"
        Title="Backup" Icon="Backup.ico" ResizeMode="NoResize" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" FontFamily="Segoe UI Semibold" FontSize="17">
    <Grid>
        <Grid.Resources>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Margin="10,10,10,0" Text="Source Drive:" TextAlignment="Center" />
        <TextBlock Grid.Column="1" Grid.Row="0" Margin="10,10,10,0" Text="Destination Drive:" TextAlignment="Center" />
        <ComboBox x:Name="cmbSource" Grid.Column="0" Grid.Row="1" Margin="10" SelectionChanged="cmbSource_SelectionChanged" ItemsSource="{Binding}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding Path=Name,Mode=OneWay}" />
                        <Run Text=" - " />
                        <Run Text="{Binding Path=VolumeLabel,Mode=OneWay}" />
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <ComboBox x:Name="cmbDestination" Grid.Column="1" Grid.Row="1" Margin="10" SelectionChanged="cmbDestination_SelectionChanged" ItemsSource="{Binding}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding Path=Name,Mode=OneWay}" />
                        <Run Text=" - " />
                        <Run Text="{Binding Path=VolumeLabel,Mode=OneWay}" />
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBox x:Name="txtSource" Grid.Column="0" Grid.Row="2" IsReadOnly="True" Width="350" Height="200" TextWrapping="Wrap" Text="{Binding Path=SourceDriveInfo, Mode=TwoWay}" ScrollViewer.VerticalScrollBarVisibility="Visible" Margin="10,0,10,0" />
        <TextBox x:Name="txtDestination" Grid.Column="1" Grid.Row="2" IsReadOnly="True" Width="350" Height="200" TextWrapping="Wrap" Text="{Binding Path=DestinationDriveInfo, Mode=TwoWay}" ScrollViewer.VerticalScrollBarVisibility="Visible" Margin="10,0,10,0" />
        <TextBlock Grid.Column="0" Grid.Row="3" Margin="10" Text="Log Location:" TextAlignment="Right" VerticalAlignment="Center" />
        <TextBox x:Name="txtLog" Grid.Column="1" Grid.Row="3" BorderBrush="Black" BorderThickness="1" Margin="10" Padding="3" Text="C:\copylog" />
        <TextBlock x:Name="lblStatus" Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="2" Margin="10,0,10,0" TextAlignment="Center" Text="{Binding Path=Status, Mode=TwoWay}" />
        <Button x:Name="btnBackup" Grid.Column="0" Grid.Row="5" Height="30" Width="120" Content="_Backup" Click="btnBackup_Click" Margin="10" HorizontalAlignment="Right" IsDefault="True" />
        <Button x:Name="btnRefresh" Grid.Column="1" Grid.Row="5" Height="30" Width="120" Content="_Refresh" Click="btnRefresh_Click" Margin="10" HorizontalAlignment="Left" />
    </Grid>
</Window>